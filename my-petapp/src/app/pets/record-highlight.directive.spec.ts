import { RecordHighlightDirective } from './record-highlight.directive';

describe('RecordHighlightDirective', () => {
  it('should create an instance', () => {
    // const directive = new RecordHighlightDirective();
    // expect(directive).toBeTruthy();
  });
});
